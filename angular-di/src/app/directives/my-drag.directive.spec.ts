import { MyDragDirective } from './my-drag.directive';

describe('MyDragDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDragDirective();
    expect(directive).toBeTruthy();
  });
});
