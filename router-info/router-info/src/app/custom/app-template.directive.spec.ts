import { AppTemplateDirective } from './app-template.directive';

describe('AppTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
